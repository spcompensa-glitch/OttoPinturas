{
  "name": "prospect-on-root",
  "version": "3.0.0",
  "description": "Root package - deploy via Railway using separate services for backend and frontend",
  "scripts": {
    "start": "echo 'Configure Root Directory in Railway: use /backend or /frontend'"
  }
}
